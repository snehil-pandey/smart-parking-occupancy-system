name: park_here_shared
description: Shared clean architecture models, repositories, routing, and theme for Park Here apps.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

